# Prompt 优化

@NanoBananaEditor\docs\google_image_gen_api\google_image_generation.md

参考官方文档的最佳实践，用户输入 prompt 之后，调用 LLM 对用户的 prompt 进行润色，然后交给用户进行二次确认和修改。使用修改后的 prompt 再传递给 google api 进行图像编辑。

在 env 里定义 openai 的 base url、apikey、model ，用来处理项目里的所有文本 AI 处理任务。

# 增加图片的上传方法

有的时候我会直接从网页上复制图片，想要直接粘贴在当前的网页里。这样的话可以避免图片先下载再上传的重复步骤。
